import {useParams} from "react-router-dom";import StatusTimeline from "../../components/reports/StatusTimeline";export default function ReportDetail(){const {id}=useParams();return <div className="ap-container ap-section"><div className="ap-eyebrow">REPORT {id}</div><h1>Issue timeline</h1><div className="ap-card" style={{maxWidth:650}}><StatusTimeline status="IN PROGRESS"/></div></div>}